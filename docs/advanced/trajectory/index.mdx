---
description: Control loops define how a robot moves autonomously.
---

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';
import useBaseUrl from '@docusaurus/useBaseUrl';
import ThemedImage from '@theme/ThemedImage';

# Trajectory

Trajectories combine the concepts of [paths](/advanced/paths) and [motion profiles](/advanced/controlloops) to create smooth, efficient, and accurate robot movements.
A trajectory defines not only the path the robot should follow, but also how fast it should move along that path at any given time.